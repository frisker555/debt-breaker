# Debt Breaker v18.44 PWA 配布・更新手順

## 1. 初回公開
このフォルダを **HTTPS対応の静的Webホスティング**へ、そのままアップロードします。

必要ファイル:
- `index.html`
- `manifest.webmanifest`
- `sw.js`
- `version.json`
- `icons/`

公開後は同じURLを継続利用してください。PWAのlocalStorageは「公開URLのオリジン」に紐づくため、ドメインや公開元を途中で変更すると保存領域も別になります。

## 2. v18.39〜v18.43のローカルHTMLから移行
1. 旧HTMLを開く。
2. 「設定」→「データ・バックアップ」→「バックアップを書き出す」でJSONを保存。
3. PWA版URLを開く。
4. 初回画面の「旧版バックアップから移行」を押す。
5. 2で保存したJSONを選ぶ。
6. 復元後、「設定」で生活費月予算・自由費月予算・外食警戒ラインを確認。

Schemaは23のままなので、Schema 23バックアップを復元できます。

## 3. ユーザーのインストール
Android / Chrome等では、アプリ内「設定」→「アプリ・アップデート」→「アプリとしてインストール」からホーム画面へ追加できます。
ブラウザ側の「ホーム画面に追加 / アプリをインストール」メニューからでも追加できます。

## 4. 次回アップデート方法
例: v18.45を公開する場合
1. `index.html` の `APP_VERSION` を `18.45` に変更。
2. `sw.js` の `APP_VERSION` を `18.45` に変更（CACHE_NAMEが変わることを確認）。
3. `version.json` の `version` を `18.45` に変更。
4. 必要なアプリ変更を反映。
5. サーバー上の同じURLへファイル一式を上書きアップロード。

ユーザー側では起動時に `version.json` を確認し、新版がある場合は「更新する」案内が表示されます。
「更新する」を押すと新Service Workerへ切り替わり、自動再読み込みします。
localStorageのユーザーデータは同じURLを維持する限りそのまま残ります。

## 5. Schemaを変更するアップデート
Schema 23 → 24のように保存構造を変更する場合は、PWA更新より先にデータ移行設計が必要です。
現在の復元処理は同一Schemaを前提とするため、Schemaを上げるだけでは旧データを安全に引き継げません。

## 6. キャッシュ方針
- `index.html` / manifest / icons: Service Workerのバージョン付きCache Storageへ保存。
- `version.json`: キャッシュせずネットワーク確認。
- 新Service Worker: 自動では即時切替せず waiting 状態にし、ユーザーが「更新する」を押した時だけ `skipWaiting()`。
- オフライン時: 現在インストール済みのアプリシェルを起動。

## 7. バックアップ推奨
PWA化後もデータは端末・ブラウザ内保存です。端末故障、ブラウザデータ消去、ドメイン変更に備え、JSONバックアップ機能は継続利用してください。
